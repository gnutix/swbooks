language: php
php:
    - 7.0
    - 5.6

before_install:
    - composer self-update

install:
    - npm install -g bower jshint

before_script:
    - composer install
    - bower install

script:
    - $(pwd)/bin/phpunit

after_script:
    - $(pwd)/bin/console cs
