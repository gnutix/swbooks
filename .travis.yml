language: php
php:
    - 5.5
    - 5.4
    - 5.3

install:
    - npm install jshint
    - npm install bower

after_install:
    - composer self-update

before_script:
    - composer install
    - bower install

script:
    - $(pwd)/bin/phpunit

after_script:
    - $(pwd)/bin/console cs
